services:
  app:
    build: .
    environment:
      - PYTHONUNBUFFERED=1
    volumes:
      - quiz-data:/app/data
      - quiz-videos:/app/videos
    restart: unless-stopped

volumes:
  quiz-data:
  quiz-videos:
